# DiscordClone — PART 0: Prerequisites and Environment Setup

This document covers **everything that must exist** before beginning the AWS infrastructure build in Part 1.  
It ensures your AWS account, GitHub repo, and development environment are correctly prepared.

---

## 🧱 Overview

DiscordClone will use:
- **AWS** → hosting & infrastructure (S3, CloudFront, later App Runner / Lambda)
- **GitHub Actions** → CI/CD automation
- **Vite + React** → frontend framework
- **Node.js + npm** → build tooling

You will provision resources via **CloudFormation**, deploy via **GitHub Actions**, and maintain least-privilege IAM credentials for automation.

---

## 🧩 1️⃣ AWS Account Setup

1. **Create an AWS account** at [https://aws.amazon.com/](https://aws.amazon.com/).  
2. **Secure the root user**  
   - Enable MFA  
   - Do **not** use the root account for deployments  
3. **Create an admin user** (console + programmatic access) — e.g. `infra-admin`  
   - Attach `AdministratorAccess`  
   - Use this only for initial setup and IAM creation (Part 1 will handle the CI user)  

---

## 🧩 2️⃣ GitHub Repository

1. Create or reuse a repo (e.g. `DiscordCloneNew`)  
2. Structure:
   ```
   DiscordCloneNew/
   ├─ frontend/               # (Part 1) Vite React app - create using the commands below
   ├─ backend/                # (Part 2)
   ├─ infra/                  # CloudFormation templates + deploy scripts
   ├─ .github/
   │   └─ workflows/          # CI/CD definitions
   └─ docs/                   # Technical documentation (this + Part 1+)
   ```

3. You’ll later add **repository secrets** for AWS credentials in Part 1.

---

## 🧩 3️⃣ Local Development Environment

Install and verify:

| Tool | Version | Purpose |
|------|----------|----------|
| Node.js | ≥ 20 | Build and run React app |
| npm | ≥ 9 | Package manager |
| Git | latest | Version control |
| AWS CLI v2 | latest | Required for Part 1 (install but don’t configure yet) |

Check:
```bash
node -v
npm -v
git --version
aws --version
```

> ⚠️ You **should not run `aws configure` yet** — the IAM user used for it will be created in Part 1.

---

## 🧩 4️⃣ Regional and Naming Conventions

- **AWS Region:** `us-west-1` (N. California)  
  → low latency for Americas, lower congestion than `us-east-1`
- **Resource naming pattern:**  
  ```
  discordclonenew-frontend-luis
  discordclonenew-backend-luis
  discordclone-ci
  ```
  This avoids S3 global name collisions and keeps resources clearly scoped.

---

## 🧩 5️⃣ Initialize Frontend Project

Inside your repo:
```bash
npm create vite@latest frontend -- --template react
cd frontend
npm install
npm run dev
```

Confirm the app builds and runs locally.

---

## ✅ At the End of Part 0

You should have:
- An AWS account with an **admin user** ready to create IAM roles/policies.
- A **GitHub repository** with basic folder structure.
- A working **Node + Vite + React** frontend.
- All required local tools installed and verified.

Next → proceed to **Part 1** to create IAM users, configure the AWS CLI, and deploy infrastructure.

---
