# --- Build stage ---
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .

# --- Run stage ---
FROM node:20-alpine
WORKDIR /app
COPY --from=build /app /app
EXPOSE 8000
CMD ["node", "index.js"]
